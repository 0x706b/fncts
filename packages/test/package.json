{
  "name": "@fncts/test",
  "type": "module",
  "types": "./build/dts",
  "scripts": {
    "build": "tsc -b ./tsconfig.build.json",
    "format:write": "prettier --list-different -w src/**/*.ts && eslint --fix src/**/*.ts",
    "circular": "madge --circular --no-spinner --warning build/esm"
  },
  "dependencies": {
    "@fncts/base": "workspace:*"
  },
  "devDependencies": {
    "@fncts/config": "workspace:*",
    "@fncts/eslint-config": "workspace:*",
    "@fncts/typelevel": "workspace:*",
    "madge": "^5.0.1",
    "typescript": "4.6.0"
  },
  "tsplus": {
    "typeDir": "./build/dts"
  }
}
